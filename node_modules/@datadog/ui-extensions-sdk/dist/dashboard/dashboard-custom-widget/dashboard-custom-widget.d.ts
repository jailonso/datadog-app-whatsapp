import { DDFeatureClient } from '../../shared/feature-client';
import type { ContextClient, LoggerClient, RequestClient, WidgetOptionItem } from '../../types';
export declare class DDDashboardCustomWidgetClient extends DDFeatureClient {
    constructor(client: ContextClient & LoggerClient & RequestClient);
    updateOptions(newOptions: WidgetOptionItem[]): Promise<unknown>;
}
