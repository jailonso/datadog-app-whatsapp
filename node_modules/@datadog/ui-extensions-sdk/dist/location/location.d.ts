import { RequestClient } from '../types';
export declare class DDLocationClient {
    private readonly client;
    constructor(client: RequestClient);
    goTo(url: string): Promise<unknown>;
}
export interface NavigateTopRequest {
    url: string;
}
