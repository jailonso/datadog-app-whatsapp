import { OrgConfig, ContextClient, LoggerClient, RequestClient } from '../types';
export declare class DDConfigClient {
    private readonly client;
    constructor(client: ContextClient & LoggerClient & RequestClient);
    getOrgConfig(): Promise<OrgConfig>;
}
